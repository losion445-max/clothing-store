spring:
  datasource:
    url: jdbc:h2:mem:shop_test
    driver-class-name: org.h2.Driver
    username: sa
    password:
    
  liquibase:
    enabled: false

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop